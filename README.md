MED-FORMS: Secure HTML → PDF Medical Converter
Сервис для безопасной генерации медицинских PDF-документов из HTML-шаблонов.

1. Что это?
MED-FORMS — это backend-сервис, который принимает HTML-шаблон + JSON-данные пациента, подставляет значения в шаблон (Mustache) и формирует готовый PDF-документ.

Используется для:
эпикризов
выписок
информированных согласий
чек-листов
протоколов
любых клинических форм

2. Для чего это?
Чтобы унифицировать и автоматизировать выдачу меддокументов:
не нужен Word
не нужен PDF-редактор
исключаются ошибки
экономится время врача
документы всегда одного формата

Можно интегрировать с:
мобильным приложением
медицинской ИС
телеграм-ботом
госпитальной системой

3. Как и с чем работает?
⚙ Стек
Node.js 24+
Fastify
Mustache — подстановка данных в HTML
pdf-lib или Puppeteer — генерация PDF
Winston / pino — логи
CleanHTML фильтрация — безопасность

📌 Что делает сервис
Принимает POST /convert
Вход: HTML-шаблон или имя шаблона + JSON
Подставляет данные в Mustache
Генерирует PDF
Возвращает бинарный файл или Base64
Проверяет длину текста
Запрещает <script> и лишние теги
Ведёт технические логи

🚀 Быстрый старт
Установить зависимости:
npm install

Запуск:
npm run dev
По умолчанию сервис поднимается на:
http://localhost:3006
📤 Пример запроса

POST /convert

{
  "template": "discharge.html",
  "data": {
    "patientName": "Иван Петров",
    "dob": "1990-02-10",
    "diagnosis": "Острое воспаление",
    "doctor": "Копылов В.Ю."
  }
}
📥 Пример HTML-шаблона

templates/discharge.html:

<h2>Эпикриз пациента {{patientName}}</h2>
<p>Дата рождения: {{dob}}</p>
<p>Диагноз: {{diagnosis}}</p>
<p>Врач: {{doctor}}</p>
📁 Структура проекта
/src
  app.js
  routes/convert.js
  services/pdfService.js
  utils/logger.js
/templates
  discharge.html
/logs
  requests.log
  errors.log
Dockerfile
.env.example
.gitignore
README.md
📄 Пример успешного ответа
{
  "status": "ok",
  "size": 24152,
  "type": "application/pdf"
}

PDF можно получить как бинарный поток (attachment) или Base64.

🐳 Docker
docker build -t med-forms .
docker run -p 3006:3006 med-forms
🔒 Безопасность

Ограничение размера HTML
Фильтрация тегов (<script> → запрещено)
Ограничение длины полей

Mustache — безопасная подстановка
Логи без персональных данных

📝 Логи
Записываются:
метод + маршрут
шаблон
время рендеринга
ошибки валидации
ошибки генерации PDF

📄 Лицензия
MIT — свободное использование, модификация и интеграция.
